#include <stdlib.h>
#include <stdio.h>
#include <ctype.h>
#include "main.h"

/**
 * _abs - a function that gives the absolute value
 * @n: integer number
 * Return: the abolsute value of a number
 */
int _abs(int n)

{
if (n > 0)
{
return (n);
}
else
{
return (-n);
}
}
