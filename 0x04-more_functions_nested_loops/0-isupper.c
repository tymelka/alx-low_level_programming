#include "main.h"

/**
* _isupper - checks if uppercase
* @c: character input
* Return: 1 if upper cases otherwise  returns 0
*/

int _isupper(int c);

{
if (c >= 65 && c <= 90)
{
return (1);
}
else
return (0);
}
}
