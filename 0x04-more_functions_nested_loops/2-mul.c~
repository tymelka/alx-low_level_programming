#include "main.h"
/**
 * _isdigit - Check if is digit
 * @x: First Integer
 * @x: Second Integer
 *
 * Return: 1 if digit otherwise 0the multiplied value
 */
int mul(int a, int b);

if (x >= 48 && x <= 57)
{
return (1);
}

return (0);
}
