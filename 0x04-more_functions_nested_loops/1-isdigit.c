#include "main.h"
/**
 * _isdigit - Check if is digit
 * @x: The number to be checked
 *
 * Return: 1 if digit otherwise 0
 */
int _isdigit(int x)
{
if (x >= 48 && x <= 57)
{
return (1);
}

return (0);
}
