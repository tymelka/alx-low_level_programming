#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/* more headers goes there */

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
  int x, chprefix;
for (x = '0'; x <= '9'; x++)
{
 if (x > '9')
   {
     chprefix=48;
   }
 else {chprefix='0';}
putchar(chprefix);
putchar(x);
putchar(44);
putchar(32);

 }
putchar ('\n');
return (0);
}
