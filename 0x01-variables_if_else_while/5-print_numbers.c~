#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/* more headers goes there */


/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)

{
int x;
for (x = 1; x <10; x++)
{
  printf("%i",x);
}
putchar ('\n');
return (0);
}
