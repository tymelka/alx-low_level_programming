#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/* more headers goes there */

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
  int n, intpart, remain, jump, pref1, pref2;

for (n = 0; n < 999; n++)
{
if (n <= 9)
{
pref1 = 48;
 pref2 = 48;
}
else
{
pref1 = 48 + n / 10;

}
 if (n < 99)
{
intpart = n / 10;
remain = n % 10;
else
{
intpart = n / 100;
remain = n % 100;    
}
/*     printf("intpart is %d remainis  %d\n", intpart, remain); */
if (remain == 0)
{
jump = intpart*10;
n = n + jump;
}
else
{
putchar (pref1);
putchar (pref2); 
putchar (n % 100 + '0');
if (n != 89)
{
putchar (44);
}
if (n != 89)
{
putchar (32);
}
}
}
putchar('\n');
return (0);
}
